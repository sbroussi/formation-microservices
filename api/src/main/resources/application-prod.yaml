spring:
  datasource:
    url: jdbc:mysql://ENV_MYSQL_SERVER:3306/gestionclient?useUnicode=true&characterEncoding=utf8&useSSL=false

app:
  pictures:
    directory: /ENV_PICTURES_FOLDER

eureka:
  client:
    serviceUrl:
      defaultZone: http://ENVEUREKASERVER:8761/eureka/
